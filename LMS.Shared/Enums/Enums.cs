namespace LMS.Shared.Enums
{
    public enum UserRole
    {
        Admin,
        Teacher,
        Student
    }

    public enum EnrollmentStatus
    {
        Active,
        Completed,
        Dropped
    }
}